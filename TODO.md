# TODO - labcab-kiosk quality fixes

- [x] Fix Firebase Firestore initialization in `src/lib/firebase.ts` to be resilient when `firestoreDatabaseId` is missing/invalid.

- [ ] Re-run TypeScript/typecheck and build (once `npm install` works) and fix any compile/runtime issues.
- [ ] Re-run `vite build` to confirm no bundling errors.

